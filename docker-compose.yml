services:
  maturity-app:
    build:
      context: .
      dockerfile: Dockerfile
      provenance: false
      sbom: false

    image: ${ECR_REGISTRY}/maturity:${BUILD_NUMBER}

    container_name: maturity

    restart: unless-stopped

    ports:
      - "7000:7000"

    environment:
      PORT: "7000"