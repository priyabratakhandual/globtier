version: "3.9"

services:
  maturity-app:
    container_name: maturity

    image: ${IMAGE_NAME}:${BUILD_NUMBER}

    restart: unless-stopped

    ports:
      - "7000:7000"

    environment:
      PORT: "7000"