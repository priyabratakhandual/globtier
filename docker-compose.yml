version: '3.8'

services:

  maturity-app:

    build: .

    image: priyabratakhandual/maturity-app:latest

    container_name: maturity-app

    restart: always

    ports:
      - "7000:7000"

    environment:
      PORT: 7000